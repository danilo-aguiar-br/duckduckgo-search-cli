{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/danilo-aguiar-br/duckduckgo-search-cli/raw/main/docs/schemas/ndjson-event.schema.json",
  "title": "NdjsonEvent",
  "description": "PLANNED / UNIMPLEMENTED: NDJSON streaming event for future --stream support (one JSON object per line). The --stream flag is not implemented as of v0.9.4; this schema is a placeholder only.",
  "type": "object",
  "required": ["event"],
  "properties": {
    "event": { "type": "string", "enum": ["begin", "match", "end", "error"], "description": "Event type: begin (file start), match (search result), end (file end), error (failure)." },
    "data": { "type": "object", "description": "Event payload (varies by event type)." }
  },
  "additionalProperties": false
}
